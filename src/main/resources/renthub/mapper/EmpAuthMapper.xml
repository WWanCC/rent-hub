<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="renthub.mapper.EmpAuthMapper">

    <select id="findPermissionKeysByEmpId" resultType="java.lang.String">
        select distinct p.permission_key
        from emp_role er
                 join role_permission rp on er.role_id = rp.role_id
                 join permission p on rp.permission_id = p.id
        where er.emp_id = #{empId}
    </select>
    <select id="findRoleKeysByEmpId" resultType="java.lang.String" >
        SELECT r.role_key
        FROM emp_role er
                 JOIN role r ON er.role_id = r.id
        WHERE er.emp_id = #{empId}
    </select>
</mapper>
