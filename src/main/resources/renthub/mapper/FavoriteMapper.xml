<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="renthub.mapper.FavoriteMapper">
    <select id="findAllUserFavorite" resultType="renthub.domain.vo.HouseVO">
        select h.id,
               h.title,
               h.image,
               h.region_name,
               h.price_per_month,
               h.status,
               h.area,
               h.layout
        from house h
                 join user_favorite f
                      on f.house_id = h.id
        where f.user_id = #{userId}
        order by f.created_at desc
    </select>
</mapper>
