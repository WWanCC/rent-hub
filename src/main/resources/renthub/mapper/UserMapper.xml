<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="renthub.mapper.UserMapper">

    <select id="queryPhoneRealNameIdentityCardIdByUserIdAfter" resultType="renthub.domain.vo.UserProfileVO">
        select user.phone, user_detail.real_name, user_detail.identity_card_id
        from user
                 join user_detail
                      on user.id = user_detail.user_id
        where user_id = #{userId}
    </select>
</mapper>
